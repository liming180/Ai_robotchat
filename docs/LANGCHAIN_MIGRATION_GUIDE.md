# LangChain ???????

## ??

????? LangChain ??????????

- **Function Calling??????**??? LangChain 1.x `create_agent()` ????????? Agent
- **RAG ??????**??? `embedding-3` + Chroma ???????????
- **????**????? / ??????????????????
- **????**??????????LangChain ?????????????

---

## ??????

### 1. ????

```bash
cd backend
pip install -r requirements.txt
```

?????

| ?? | ?? |
| --- | --- |
| `langchain` | ?????????? 1.x? |
| `langchain-openai` | OpenAI ????????? GLM? |
| `langchain-community` | ???????DuckDuckGo ?? |
| `langchain-chroma` / `chromadb` | ????RAG? |
| `langchain-text-splitters` | ???? |
| `ddgs` | ???? `web_search` ????? |
| `zhipuai` | ?? SDK?Embedding ?? |

### 2. ??????

? `backend/.env` ????

```env
# ????? API Key
ZHIPU_API_KEY=your_api_key_here

# ??
ZHIPU_MODEL=glm-4.5-air
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# ????????????????? SQLite?
DATABASE_URL=sqlite:///./ai_companion.db

# Redis???????????????
REDIS_URL=redis://localhost:6379/1
```

> **????**?`API_KEY` ?????????`app/ai/config.py` ?????????
> `.env` ?? `.gitignore` ??????????

### 3. ???????

```bash
cd backend
python init_db.py
```

### 4. ????

```bash
cd backend
python main.py
```

?????????

```
Successfully initialized Zhipu AI client with model: glm-4.5-air
[RAG] ? Embedding ????? 2048??????????
[RAG] Chroma ??????: .../backend/chroma_db
[LangChain] Agent ready, tools: ['web_search', 'search_user_memory', 'get_mood_history', 'add_user_memory', 'search_knowledge']
Successfully initialized LangChain client with model: glm-4.5-air
```

---

## ??????

### 1. `app/ai/langchain_client.py` ? Agent ???

**????**??? `create_agent()` ?? Agent???????????**????**?

```python
self.agent = create_agent(
    model=self.llm,
    tools=self.tools,
    system_prompt=build_system_prompt(),
)
```

> ?? **??**????? `chain = prompt | self.llm` ?????
> ??? tools ????????????????"??????"??????
> ????????? ?? ?????"??"????????

**????**?

| ??? | ?? | ???? |
| --- | --- | --- |
| `web_search` | ???????? | DuckDuckGo |
| `search_user_memory` | ?????? | `user_memories` ? **+** ??? |
| `get_mood_history` | ?????? | `user_moods` ? |
| `add_user_memory` | ?????? | ?????? **+** ??? |
| `search_knowledge` | ??????? | Chroma ??? |

**????**???????????`main.py` ??????

```python
client.chat(user_input, chat_history=None, personality="????", user_id=None) -> dict
async for chunk in client.chat_stream(user_input, chat_history=None, personality="????", user_id=None): ...
```

### 2. `app/ai/rag.py` ? RAG ???

????????? + ?????????

```
build_embeddings()
  ?? FallbackEmbeddings???????????????
  ?    ?? ????ZhipuEmbeddings?embedding-3?2048 ??
  ?    ?? ?????SimpleHashEmbeddings?????????????
  ?? ???? Chroma?persist_directory = backend/chroma_db?
```

**???????**?Embedding ?????????????????????
`FallbackEmbeddings` ?????**??????**??????????
?????????????????????????????????????

**????**?

```python
from app.ai.rag import get_rag_store

store = get_rag_store()            # ?????????? None???????
store.add_memory(user_id, content, memory_type="fact")
store.add_knowledge(title, content, category="general")
docs = store.search("???", user_id="u1", k=4)   # -> List[Document]
print(store.backend)               # ?? "chroma+zhipu" ? "chroma+hash_fallback"
```

**????**?? Chroma ???????????????`memory_fallback`??
?????????????

---

## ??API ??

### ??????????

| ?? | ?? |
| --- | --- |
| `POST /api/v1/ai/chat` | ???? |
| `POST /api/v1/ai/chat/stream` | ???? |
| `POST /api/v1/ai/generate-avatar` | ???? |

### LangChain ????

| ?? | ?? |
| --- | --- |
| `POST /api/v1/ai/chat/langchain` | LangChain ???Function Calling? |
| `POST /api/v1/ai/chat/langchain/stream` | LangChain ?????**???? Function Calling**? |

### ???????

| ?? | ?? |
| --- | --- |
| `GET /api/v1/memory/{user_id}` | ????????? `?keyword=`? |
| `POST /api/v1/memory` | ?????? |
| `GET /api/v1/mood/{user_id}` | ????????? `?days=7`? |
| `POST /api/v1/mood` | ?????? |

> ?? `userId` ??Agent ????? Function Calling ???????????????

---

## ??????

### ????

```bash
curl -X POST "http://localhost:5000/api/v1/ai/chat/langchain" \
-H "Content-Type: application/json" \
-d '{
  "userMessage": "???",
  "systemPrompt": "?????????AI??",
  "messages": []
}'
```

### Function Calling?????????

```bash
# 1. ????
curl -X POST "http://localhost:5000/api/v1/memory" \
-H "Content-Type: application/json" \
-d '{
  "userId": "user-001",
  "memoryType": "fact",
  "content": "?????????5?20?"
}'

# 2. ??????AI ??? search_user_memory?
curl -X POST "http://localhost:5000/api/v1/ai/chat/langchain" \
-H "Content-Type: application/json" \
-d '{
  "userMessage": "?????????",
  "userId": "user-001",
  "messages": []
}'
```

### ??????

```bash
curl -X POST "http://localhost:5000/api/v1/mood" \
-H "Content-Type: application/json" \
-d '{"userId":"user-001","mood":"??","score":8.5,"note":"?????"}'

curl -X POST "http://localhost:5000/api/v1/ai/chat/langchain" \
-H "Content-Type: application/json" \
-d '{"userMessage":"?????????","userId":"user-001","messages":[]}'
```

---

## ??????

### ?????

? `langchain_client.py` ? `_init_tools()` ???????

```python
@tool
def my_new_tool(param: str) -> str:
    """????????????????????????"""
    return "??????"

tools.append(my_new_tool)
```

> ???`@tool` ?????**??? docstring**??? LangChain ??????

### ????????RAG?

```python
from app.ai.rag import get_rag_store

store = get_rag_store()
store.add_knowledge(
    title="????",
    content="????????????",
    category="manual",
)
```

????? `RecursiveCharacterTextSplitter` ?????????

---

## ??????

```
????
  ??? LangChain Agent ???
  ?    ?? ? ? Agent ?????????
  ?    ?         ?? ?? ? ????
  ?    ?? ? ? ?? zhipu_client
  ?               ?? ?? ? ????
  ??? ????
```

RAG ???????

| ?? | ??? | ???? |
| --- | --- | --- |
| Embedding | ?? `embedding-3` | ?????? |
| ???? | Chroma ??? | ???? |
| ??? | SQLAlchemy | ????????????? |

---

## ??????

- [X] ???????????????????????
- [X] LangChain Agent ??????????????
- [X] Function Calling ???????????? `tool_calls` ? SQL ???
- [X] RAG ??????`store.backend` ?? `chroma+zhipu`?
- [X] ??????????
- [X] Embedding ??????????????
- [X] ?????????? + ????

**????**?? `backend/` ?????

```bash
python -c "from app.ai.rag import get_rag_store; s=get_rag_store(); print(s.backend)"
python -c "from app.ai.langchain_client import LangChainGLMClient; print([t.name for t in LangChainGLMClient().tools])"
```

---

## ??????

1. **???? `create_agent`**??? `prompt | llm` ????????????
2. **`@tool` ??? docstring**???? `ValueError`?
3. **????????**?`config.py` ??????????`ZhipuAIConfig()` ?
   `ZhipuAIConfig.from_env()` ?? `ZHIPU_API_KEY` ???
4. **Token ??**?Agent ????????Token ?????? Chain?
5. **Redis ??**??????????????
6. **???????**?`chroma_db/`?`ai_companion.db`?`.env` ??? `.gitignore` ??
7. **???????**????? PostgreSQL ??? `UUID` / `ARRAY` ??
   ??? MySQL/SQLite ???? `String(36)` / `JSON`?

---

## ??????

- [LangChain ????](https://python.langchain.com/)
- [?? AI ??](https://open.bigmodel.cn/dev/api)
- [??????](./01-????????.md)
